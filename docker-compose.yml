services:
  db:
    image: postgres:18
    container_name: it_courses
    environment:
      POSTGRES_DB: it_courses
      POSTGRES_USER: it_user
      POSTGRES_PASSWORD: it_password
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wh_user -d wh"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
